"```json\n[\n    {\n        \"task\": \"Create a regular expression to validate AWS S3 bucket names. S3 bucket names must be between 3 and 63 characters, contain only lowercase letters, numbers, hyphens, and periods, start and end with a letter or number, and cannot contain consecutive periods or hyphens followed by periods.\"\n    },\n    {\n        \"task\": \"Write a Python function that takes an AWS CloudWatch log message as a string and extracts the timestamp, log level (ERROR, WARN, INFO, DEBUG), and message content. The log format is: '[TIMESTAMP] [LOG_LEVEL] message text'.\"\n    },\n    {\n        \"task\": \"Create a JSON object representing an AWS IAM policy that allows a user to read all objects from an S3 bucket named 'my-app-bucket' and list the bucket contents, but denies all other S3 actions.\"\n    }\n]\n```"